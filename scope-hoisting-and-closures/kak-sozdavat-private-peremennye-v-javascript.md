# Как создавать private-переменные в JavaScript?

Самый частый способ — через замыкание.

Переменная живет во внешней функции и недоступна напрямую снаружи. Снаружи доступны только методы, которые с ней работают.

В классах также можно использовать приватные поля с `#`.
