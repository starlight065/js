# Что такое замыкание (closure)?

**Closure** — это функция, которая помнит переменные из внешней области видимости даже после завершения внешней функции.

Используют, когда нужно:

* хранить состояние между вызовами;
* делать приватные данные;
* создавать функции с заранее заданными параметрами.
