# Что означают следующие символы в регулярных выражениях: +, \*, ?, ^, $?

`+` означает один или больше повторов,

`*` — предыдущий элемент может повторяться 0 или более раз,

`?` — ноль или одно вхождение.

`^` указывает начало строки, а `$` — её конец.

