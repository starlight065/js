# Что такое состояние гонки (race condition) в JavaScript?

JS однопоточный, поэтому "гонки потоков" как в Java/C++ нет. Но race condition всё равно возникает из-за **асинхронности** (event loop, промисы, таймеры, сетевые запросы) — порядок завершения асинхронных операций не гарантирован.

